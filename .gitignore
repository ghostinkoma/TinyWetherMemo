# --- firmware build artifacts ---
*.o
*.elf
*.bin
*.hex
*.lst
*.map
firmware/main_ext.bin

# PlatformIO
.pio/
firmware/.pio/

# ch32fun brought in for building (symlink/copy; not vendored)
firmware/ch32fun/

# scratch / stray
Souce/
**/新しいテキスト ドキュメント.txt

# OS / editor
Thumbs.db
.DS_Store
*.swp

# --- secrets (公開しない。テンプレは *.example / *_sample.h を追跡) ---
# 実 WiFi 資格情報など
host_esp32c3/src/config.local.h
# デバイス固有 TLS 秘密鍵 (tools/gen_cert.ps1 で生成)
host_esp32c3/src/cert_pem.h
# ログイン認証ハッシュ (デバイス実行時生成) / 実行時設定
**/auth.ini
**/settings.ini
# 引き継ぎメモ (実IP/MAC/COM/既定資格情報など運用機密を含む=公開しない)
/DISPATCH.md

# --- server (ロリポップ SQL 連携): 実 DB 資格情報 / 秘密鍵 / 認証コードは公開しない ---
# テンプレ config.example.py のみ追跡する
server/config.local.py
server/config.local.php
server/**/*.local.py
server/**/*.local.php
# Python venv / キャッシュ
server/venv/
server/.venv/
**/__pycache__/
*.pyc
